function findMissing(arr1, arr2) {
    arr1.forEach(element => {
        if (arr1.filter((el) => {element == el}).length == 0 == ) {
            console.log(element)
        }
    });
}

arr1 = [1, 2, 3]
arr2 = [1, 3]
